import MatrixConcentration

/-!
Package-scoped linter harness for V8.

The explicit namespace scope is the calibration: a bare `#lint` here would
inspect only declarations in this import-only harness.
-/

#lint in MatrixConcentration
